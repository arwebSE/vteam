checks:
    javascript: true

build:
    environment:
        node: "20"

    nodes:
        coverage:
            dependencies:
                before:
                    - nvm install 20
                    - nvm use 20
                    - cd backend
                    - npm install
            tests:
                override:
                    - command: "npm test"
                      coverage:
                          file: "backend/coverage/clover.xml"
                          format: clover
        analysis:
            tests:
                override:
                    - js-scrutinizer-run
        tests: true
filter:
    excluded_paths:
        - "backend/coverage/lcov-report"
